# frozen_string_literal: true

Bundler::GemHelper.install_tasks name: 'ulysses'
Bundler::GemHelper.install_tasks name: 'ulysses-nanoc'
require 'rspec/core/rake_task'

RSpec::Core::RakeTask.new(:spec)

task default: :spec
